<view class="map-container">
  <map 
    class='map' 
    scale="12" 
    latitude="{{latitude}}"
    longitude="{{longitude}}"
    markers="{{markers}}"
    show-location
    bindtap='mapClick'
    bindregionchange="regionChange"
    id="myMap"></map> 
</view>